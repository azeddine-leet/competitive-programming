#include <iostream>
using namespace std;
int	main()
{
	int lon_cote;
	lon_cote = (17 * 5) + (7 * 2) + 5 + (2 * 2);
	cout << (lon_cote * lon_cote) << endl;
	cout << (lon_cote * 4) << endl;
	
	return (0);
}

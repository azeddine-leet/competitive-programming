#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;
int main()
{
	//cout << fixed << setprecision(0);

	double a = 0 ,b = 0;
	cin >> a >>b;
	//double bb =(double (a + b))/2;
	cout << ceil((a + b)/2) << endl;
	return (0);
}
